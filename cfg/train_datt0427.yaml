headless: true
use_local_usd: false

sim: ${task.sim}
env: ${task.env}

# model_dir: /root/SimpleFlight/scripts/outputs/track_datt/04-11_02-14/wandb/latest-run/files/checkpoint_1206124544.pt
model_dir: null

total_frames: 2000_000_000
max_iters: -1
eval_interval: -1
save_interval: 100
seed: 0

viewer:
  resolution: [960, 720]
  eye: [0., 2.0, 2.5]
  lookat: [0., 0., 1.0]

wandb:
  group: ${oc.select:..task.name}
  run_name: track_datt0427   # 原值: track_datt
  job_type: train
  entity: chen_zongy-tsinghua-university
  project: sim2real
  mode: online
  run_id:
  monitor_gym: True
  tags:

defaults:
  - task: TrackDATT0427    # 原值: TrackDATT
  - algo: mappo
  - _self_

algo:
  entropy_coef: 0.01
  clip_param: 0.1
